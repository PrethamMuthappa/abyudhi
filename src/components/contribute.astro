---
import Header from "./Header.astro"
---
<Header />  

<main class="min-h-screen bg-gradient-to-b from-gray-900 to-gray-800 text-white py-8 sm:py-12 md:py-16">
    <div class="container mx-auto px-4 sm:px-6">
        <div class="text-center mb-8 sm:mb-12 md:mb-16">
            <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold mb-3 sm:mb-4 bg-clip-text text-transparent bg-gradient-to-r from-yellow-400 to-yellow-600">
                Support Our Cause
            </h1>
            <p class="text-lg sm:text-xl text-gray-300 max-w-2xl mx-auto px-4 mt-14">
                Your contribution can make a real difference in transforming lives across India.
            </p>
        </div>

        {/* Bank Transfer Section */}
        <div class="mb-12 sm:mb-16">
            <h2 class="text-2xl sm:text-3xl font-bold text-center mb-8 sm:mb-12 text-yellow-400">
                Donate By NEFT Or RTGS
            </h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 sm:gap-8">
                {/* Axis Bank */}
                <div class="bg-white/10 backdrop-blur-sm rounded-xl p-4 sm:p-6 hover:bg-white/15 transition-all">
                    <img src="/axis-bank-logo.png" alt="Axis Bank" class="h-8 sm:h-12 mb-4 sm:mb-6 mx-auto" />
                    <div class="space-y-3 sm:space-y-4 text-gray-200">
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">A/C Holder Name:</p>
                            <p class="text-sm sm:text-base font-semibold">Raise India Foundation</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">A/C No.:</p>
                            <p class="text-sm sm:text-base font-semibold break-all">916010019605596</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">IFSC Code:</p>
                            <p class="text-sm sm:text-base font-semibold">UTIB0000696</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">Address:</p>
                            <p class="text-sm sm:text-base font-semibold">Uttam Nagar, New Delhi 110059</p>
                        </div>
                    </div>
                </div>

                {/* Kotak Bank */}
                <div class="bg-white/10 backdrop-blur-sm rounded-xl p-4 sm:p-6 hover:bg-white/15 transition-all">
                    <img src="/kotak-bank-logo.png" alt="Kotak Bank" class="h-8 sm:h-12 mb-4 sm:mb-6 mx-auto" />
                    <div class="space-y-3 sm:space-y-4 text-gray-200">
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">A/C Holder Name:</p>
                            <p class="text-sm sm:text-base font-semibold">Raise India Foundation</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">A/C No.:</p>
                            <p class="text-sm sm:text-base font-semibold break-all">4612861721</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">IFSC Code:</p>
                            <p class="text-sm sm:text-base font-semibold">KKBK0004614</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">Address:</p>
                            <p class="text-sm sm:text-base font-semibold">Property Bearing No. Thana Road, Najafgarh New Delhi 110043</p>
                        </div>
                    </div>
                </div>

                {/* Yes Bank */}
                <div class="bg-white/10 backdrop-blur-sm rounded-xl p-4 sm:p-6 hover:bg-white/15 transition-all md:col-span-2 lg:col-span-1 md:max-w-md lg:max-w-none mx-auto w-full">
                    <img src="/yes-bank-logo.png" alt="Yes Bank" class="h-8 sm:h-12 mb-4 sm:mb-6 mx-auto" />
                    <div class="space-y-3 sm:space-y-4 text-gray-200">
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">A/C Holder Name:</p>
                            <p class="text-sm sm:text-base font-semibold">Raise India Foundation</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">A/C No.:</p>
                            <p class="text-sm sm:text-base font-semibold break-all">052288700000082</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">IFSC Code:</p>
                            <p class="text-sm sm:text-base font-semibold">YESB0000522</p>
                        </div>
                        <div>
                            <p class="text-xs sm:text-sm text-gray-400">Address:</p>
                            <p class="text-sm sm:text-base font-semibold">Janak Puri, New Delhi 110058</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {/* QR Code Section */}
        <div class="text-center">
            <h2 class="text-2xl sm:text-3xl font-bold mb-8 sm:mb-12 text-yellow-400">
                Donate Using QR Code
            </h2>
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 sm:gap-8 max-w-2xl mx-auto px-4">
                {/* UPI QR */}
                <div class="bg-white/10 backdrop-blur-sm rounded-xl p-4 sm:p-6 hover:bg-white/15 transition-all">
                    <div class="aspect-square max-w-[200px] sm:max-w-[250px] mx-auto mb-3 sm:mb-4">
                        <img src="/upi-qr.png" alt="UPI QR Code" class="w-full h-full object-contain" />
                    </div>
                    <p class="text-base sm:text-lg font-semibold text-gray-200">UPI QR Code</p>
                </div>

                {/* Paytm QR */}
                <div class="bg-white/10 backdrop-blur-sm rounded-xl p-4 sm:p-6 hover:bg-white/15 transition-all">
                    <div class="aspect-square max-w-[200px] sm:max-w-[250px] mx-auto mb-3 sm:mb-4">
                        <img src="/paytm-qr.png" alt="Paytm QR Code" class="w-full h-full object-contain" />
                    </div>
                    <p class="text-base sm:text-lg font-semibold text-gray-200">Paytm QR Code</p>
                </div>
            </div>
        </div>
    </div>
</main>

<style>
    .container {
        max-width: 1200px;
    }
    
    @media (max-width: 640px) {
        .container {
            padding-left: 1rem;
            padding-right: 1rem;
        }
    }
</style>
